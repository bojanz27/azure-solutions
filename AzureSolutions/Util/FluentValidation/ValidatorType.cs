namespace AzureSolutions.Util.FluentValidation
{
    public static class ValidatorType
    {
        // use Validator<T>.IsTypeOf(...) ?

        //public static Validator<Type> IsSameType(this Validator<Type> validator, Type other)
        //{
        //    if (validator.Value.GetType() != other.GetType())
        //        throw new ArgumentException($"{validator.Name ?? "value"} is not of type '{other.ToString()}'.");
        //    return validator;
        //}
    }
}
